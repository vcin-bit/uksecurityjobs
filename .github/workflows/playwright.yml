name: Playwright Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Run Playwright Tests
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: app/package-lock.json

      - name: Install dependencies
        working-directory: app
        run: npm ci

      - name: Install Playwright browsers
        working-directory: app
        run: npx playwright install chromium --with-deps

      - name: Run marketing site tests
        working-directory: app
        run: npx playwright test tests/marketing.spec.ts
        env:
          MARKETING_BASE_URL: https://www.uksecurityjobs.co.uk

      - name: Run auth tests
        working-directory: app
        run: npx playwright test tests/auth.spec.ts
        env:
          TEST_BASE_URL: https://app.uksecurityjobs.co.uk

      - name: Run profile tests
        working-directory: app
        run: npx playwright test tests/profile.spec.ts
        env:
          TEST_BASE_URL: https://app.uksecurityjobs.co.uk
          TEST_EMAIL: ${{ secrets.TEST_EMAIL }}
          TEST_PASSWORD: ${{ secrets.TEST_PASSWORD }}

      - name: Run employer tests
        working-directory: app
        run: npx playwright test tests/employer.spec.ts
        env:
          TEST_BASE_URL: https://app.uksecurityjobs.co.uk
          TEST_EMAIL: ${{ secrets.TEST_EMAIL }}
          TEST_PASSWORD: ${{ secrets.TEST_PASSWORD }}

      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report
          path: app/playwright-report/
          retention-days: 14

      - name: Upload screenshots on failure
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: failure-screenshots
          path: app/test-results/
          retention-days: 7
